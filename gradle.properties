org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# false = app/libs/posrouter-release.aar (partner default)
# true  = composite build ../sdk-android (internal SDK dev)
useCompositeSdk=false

# true = GitHub Packages Maven (requires gpr.user / gpr.key in local.properties)
useMavenSdk=false
posrouterVersion=1.6.5
